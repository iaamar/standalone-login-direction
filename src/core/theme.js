import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#6c63ff",
    secondary: "#000000",
    error: "#ff0000",
    success: "#6be600",
    white: "#ffffff",
    grey: "#808080",
  },
};
